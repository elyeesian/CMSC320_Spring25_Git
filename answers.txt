Q1: Why might we consider using Git?

To keep track of the versions of an app, so we may easily revert to a previous version if something breaks. Additionally, we can also allow for multiple programers to work on the same piece of code concurrently without overwriting each other.
